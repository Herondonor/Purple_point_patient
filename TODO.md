# TODO - DB init automation

- [x] Implement db_init.js auto-run on server startup (when starting localhost).
- [ ] Verify by running `npm start` and checking console logs for `MySQL schema init complete`.

